import { ReactOnViewDirective } from './react-on-view.directive';

describe('ReactOnViewDirective', () => {
  it('should create an instance', () => {
    const directive = new ReactOnViewDirective(undefined);
    expect(directive).toBeTruthy();
  });
});
